# Find Android analogues for Apple Store applications
An application to find Android analogues for Apple Store applications.

## Usage
1. Clone repository
2. Install dependencies with `make code.deps`
3. Subscribe to the required RapidAPI: https://rapidapi.com/danielamitay/api/app-stores/
4. Run application with `apple-to-google`
